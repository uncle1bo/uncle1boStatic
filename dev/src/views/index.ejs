<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Markdown转HTML工具</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-10">
        <div class="card shadow-sm">
          <div class="card-header bg-primary text-white">
            <h1 class="h3 mb-0">Markdown转HTML工具</h1>
          </div>
          <div class="card-body">
            <div class="alert alert-info">
              <i class="bi bi-info-circle-fill me-2"></i>
              此工具可以将Markdown内容转换为HTML页面，并自动添加头部和尾部模板，生成多语言支持文件。
            </div>
            
            <ul class="nav nav-tabs" id="myTab" role="tablist">
              <li class="nav-item" role="presentation">
                <button class="nav-link active" id="input-tab" data-bs-toggle="tab" data-bs-target="#input" type="button" role="tab" aria-controls="input" aria-selected="true">
                  <i class="bi bi-pencil-square me-1"></i> 直接输入
                </button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="upload-tab" data-bs-toggle="tab" data-bs-target="#upload" type="button" role="tab" aria-controls="upload" aria-selected="false">
                  <i class="bi bi-upload me-1"></i> 上传文件
                </button>
              </li>
            </ul>
            
            <div class="tab-content mt-3" id="myTabContent">
              <!-- 直接输入标签页 -->
              <div class="tab-pane fade show active" id="input" role="tabpanel" aria-labelledby="input-tab">
                <form id="convertForm">
                  <div class="row mb-3">
                    <div class="col-md-4">
                      <label for="pageName" class="form-label">页面名称 <span class="text-danger">*</span></label>
                      <input type="text" class="form-control" id="pageName" name="pageName" required placeholder="例如: blog">
                      <div class="form-text">不要包含.html后缀，只使用字母、数字和连字符</div>
                    </div>
                    <div class="col-md-8">
                      <label for="pageTitle" class="form-label">页面标题</label>
                      <input type="text" class="form-control" id="pageTitle" name="pageTitle" placeholder="例如: 我的博客">
                    </div>
                  </div>
                  
                  <div class="mb-3">
                    <label for="pageDescription" class="form-label">页面描述</label>
                    <input type="text" class="form-control" id="pageDescription" name="pageDescription" placeholder="简短的页面描述">
                  </div>
                  
                  <div class="mb-3">
                    <label for="markdownContent" class="form-label">Markdown内容 <span class="text-danger">*</span></label>
                    <textarea class="form-control" id="markdownContent" name="markdownContent" rows="10" required placeholder="# 标题\n\n这是一段Markdown内容..."></textarea>
                  </div>
                  
                  <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">
                      <i class="bi bi-arrow-right-circle me-1"></i> 转换并生成页面
                    </button>
                  </div>
                </form>
              </div>
              
              <!-- 上传文件标签页 -->
              <div class="tab-pane fade" id="upload" role="tabpanel" aria-labelledby="upload-tab">
                <form id="uploadForm" enctype="multipart/form-data">
                  <div class="row mb-3">
                    <div class="col-md-4">
                      <label for="uploadPageName" class="form-label">页面名称 <span class="text-danger">*</span></label>
                      <input type="text" class="form-control" id="uploadPageName" name="pageName" required placeholder="例如: blog">
                      <div class="form-text">不要包含.html后缀，只使用字母、数字和连字符</div>
                    </div>
                    <div class="col-md-8">
                      <label for="uploadPageTitle" class="form-label">页面标题</label>
                      <input type="text" class="form-control" id="uploadPageTitle" name="pageTitle" placeholder="例如: 我的博客">
                    </div>
                  </div>
                  
                  <div class="mb-3">
                    <label for="uploadPageDescription" class="form-label">页面描述</label>
                    <input type="text" class="form-control" id="uploadPageDescription" name="pageDescription" placeholder="简短的页面描述">
                  </div>
                  
                  <div class="mb-3">
                    <label for="markdownFile" class="form-label">Markdown文件 <span class="text-danger">*</span></label>
                    <input class="form-control" type="file" id="markdownFile" name="markdownFile" accept=".md,.markdown,text/markdown">
                  </div>
                  
                  <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">
                      <i class="bi bi-cloud-upload me-1"></i> 上传并生成页面
                    </button>
                  </div>
                </form>
              </div>
            </div>
            
            <!-- 结果区域 -->
            <div id="resultArea" class="mt-4 d-none">
              <div class="alert alert-success">
                <h5><i class="bi bi-check-circle-fill me-2"></i> 转换成功！</h5>
                <p id="resultMessage"></p>
                <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                  <button id="viewPageBtn" class="btn btn-sm btn-outline-success">
                    <i class="bi bi-eye me-1"></i> 查看页面
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Bootstrap JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <!-- Custom JavaScript -->
  <script src="/js/main.js"></script>
</body>
</html>